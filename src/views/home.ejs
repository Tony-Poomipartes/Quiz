<%- include("partials/header") %>

  <main class="container py-4">
    
    <!-- Welcome -->
    <div class="text-center">
      <h2 class="h1 my-4">Bienvenue sur Quizzz</h2>
      <p class="lead">Quizzz" est un site de quiz qui vous permet de tester vos connaissances sur des sujets variés tels que les animaux célèbres, le chocolat, Linux, Star Wars, les bières belges et les fromages de France. Chaque thème comporte des questions à choix multiples allant des plus simples aux plus difficiles, afin de vous permettre d'évaluer votre niveau de connaissances. Découvrez des anecdotes intéressantes sur chaque sujet, apprenez-en davantage sur des domaines que vous ne connaissez peut-être pas et amusez-vous tout en vous cultivant.</p>
    </div>

    <!-- Cards -->
    <div class="row mb-3 text-center flew-wrap">

        <% for (const quiz of quizzes) { %>
            <!-- One card -->
            <div class="col-4">
                <div class="card mb-4">
                    <div class="card-header">
                        <h3 class="h2"><%= quiz.title %></h3>
                    </div>
                    <div class="card-body d-flex flex-column justify-content-between">
                        <h4 class="h3"><%= quiz.description %></h4>
                        <ul class="list-unstyled m-3">
                          <li>par <%= quiz.author.fullname %></li>
                        </ul>

                        <ul class="list-unstyled mb-4 p-0">
                            <% for (const tag of quiz.tags) { %>
                                <li class="badge bg-primary"><%= tag.name %></li>
                            <% } %>
                        </ul>

                        <a href="/quiz/<%= quiz.id %>" class="btn btn-lg btn-primary text-white">Jouer</a>
                    </div>
                </div>
            </div>
        <% } %>

    </div>
  </main>

<%- include("partials/footer") %>
